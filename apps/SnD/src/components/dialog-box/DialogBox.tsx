import React from 'react';
import { ModalDialog } from 'react-bootstrap';

const DialogBox = (props: any) => {
    return (
        <ModalDialog centered size="sm">
            dialog
        </ModalDialog>
    );
};

export default DialogBox;
